# 规则准入审查

## 候选规则

- 规则 ID:
- 规则标题:
- 审查人:
- 审查日期:

## 准入决策

- 决策:
  - `keep-in-semantic-review`（保留在语义审查）
  - `migrate-to-deterministic`（迁移到确定性工具）
  - `reject-from-semantic-review`（拒绝进入语义审查）

## 决策理由

为什么这条规则应该属于这个分类？

## 确定性测试

- 这条规则能否表达为包依赖、AST、命名、注解或格式化器逻辑？
- 如果可以，应该由哪个引擎执行？
- 如果不可以，还剩下什么语义判断必须交给 Qoder 审查？

## 重叠与冲突检查

- 已审查的相关规则:
- 是否发现冲突:
- 是否需要替代关系:

## Prompt 预算检查

- 这条规则是否会制造大范围低信号 prompt 噪声？
- 是否可以只在特定 scope 或 objective 下加载？

## 结论

- 是否批准:
- 合并前必须补充的修改:
- 后续确定性工具工作项:
